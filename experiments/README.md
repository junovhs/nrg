# experiments/

Anything executable: code, notebooks, scripts, and reproducible runs.

- One folder per experiment: `experiments/<slug>/`.
- Each experiment folder gets a `README.md` covering: goal, how to run, and results.
- Pin dependencies (e.g. `requirements.txt`, `environment.yml`, lockfiles) so runs reproduce.
- Link results back to the topic that motivated the experiment.
