# Decisions

Short, dated records of non-obvious choices about the repo or research direction.
Newest first.

## 2026-06-26 — Repository structure
Set up `nrg` as an AI-agent-navigable research repo: topic folders under `topics/`,
shared `sources/`, cross-topic `notes/`, `experiments/`, and `meta/`. Entry points
are `README.md` (humans) and `AGENTS.md` (agents), with `INDEX.md` as the live map.
